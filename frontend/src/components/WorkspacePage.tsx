import { useEffect,useState } from 'react';
import { api } from '../api';
type WorkspaceFeed={entity_counts:Array<{entity_code:string;count:number}>;approvals_pending:Array<unknown>;workflows_needing_action:Array<unknown>;recent_events:Array<{event_type:string;entity_type?:string;occurred_at:string}>;recent_rule_firings:Array<{rule_name:string;executed_at:string}>;report_count:number};
export function WorkspacePage({token,onOpenObjects}:{token:string;onOpenObjects:()=>void}){const[feed,setFeed]=useState<WorkspaceFeed|null>(null);const[error,setError]=useState<string|null>(null);useEffect(()=>{void api<WorkspaceFeed>('/analytics/home',token).then(setFeed).catch(e=>setError(e instanceof Error?e.message:'Unable to load workspace'))},[token]);return <main className="shell"><header className="hero"><div><p className="eyebrow">2TO / EOS</p><h1>Operating workspace</h1><p className="muted">Attention, approvals, business activity, and source-backed operating context.</p></div><button onClick={onOpenObjects}>Open business objects</button></header>{error&&<div className="error" role="alert">{error}</div>}{!feed&&!error&&<p className="muted">Loading your workspace…</p>}{feed&&<div className="grid"><section className="card"><h2>My approvals</h2><p className="metric">{feed.approvals_pending.length}</p></section><section className="card"><h2>Work needing action</h2><p className="metric">{feed.workflows_needing_action.length}</p></section><section className="card"><h2>Saved reports</h2><p className="metric">{feed.report_count}</p></section><section className="card"><h2>Business objects</h2><ul>{feed.entity_counts.map(x=><li key={x.entity_code}>{x.entity_code}: {x.count}</li>)}</ul></section></div>}</main>}
