import { useEffect,useState } from 'react';
import { api } from '../api';
import type { Field,LookupItem } from '../types';
export function RelationField({token,field,value,onChange}:{token:string;field:Field;value:string;onChange:(value:string)=>void}){const[items,setItems]=useState<LookupItem[]>([]);const[query,setQuery]=useState('');const[busy,setBusy]=useState(false);const[error,setError]=useState<string|null>(null);useEffect(()=>{let active=true;const timer=window.setTimeout(async()=>{if(!field.target_entity)return;setBusy(true);try{const suffix=query.trim()?`?q=${encodeURIComponent(query.trim())}&limit=25`:'?limit=25';const data=await api<LookupItem[]>(`/entities/${field.target_entity}/lookup${suffix}`,token);if(active)setItems(data)}catch(e){if(active)setError(e instanceof Error?e.message:'Unable to load choices')}finally{if(active)setBusy(false)}},250);return()=>{active=false;window.clearTimeout(timer)}},[field.target_entity,query,token]);return <div className="relation-field">{error&&<div className="error" role="alert">{error}</div>}<input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search related records"/><select value={value} onChange={e=>onChange(e.target.value)} disabled={busy}>{items.map(i=><option key={i.id} value={i.id}>{i.label}</option>)}</select></div>}
